# Milvus client (for Docker Milvus)
# Note: If using milvus-lite (local, no Docker), use: pip install pymilvus[model,milvus_lite]
pymilvus>=2.4.0

# Sentence transformers for embeddings
sentence-transformers>=2.0.0

# NLP for BM25
nltk>=3.9.0
rank-bm25>=0.2.2

# HTTP client for LLM APIs
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# Optional: for API-based LLM
# openai>=1.0.0
# anthropic>=0.20.0

# Streamlit UI
streamlit>=1.28.0
